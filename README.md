# Lab Clicker – простая игровая сцена

## Жанр
**Idle Clicker** с научной тематикой. Игрок нажимает на лабораторную колбу, чтобы зарабатывать очки исследований и развивать лабораторию с помощью автокликов.

## Описание геймплея
Вы оказываетесь в лаборатории, где главная цель — производить как можно больше исследовательских очков, нажимая на колбу.  
Каждое нажатие приносит ресурсы, а со временем можно приобрести автоматические улучшения, которые производят очки сами по себе.

## Тематика
- Сцена стилизована под научную лабораторию.
- Игровая механика построена вокруг "капель" исследований и научных помощников.
  
![Демонстрация](Gif/gameplay.gif)

## Игровые механики
- Нажатие на колбу вручную (ручной клик) добавляет +1 очко и небольшой бонус от текущих автокликов.
- Покупка апгрейдов (автокликов), генерирующих ресурсы каждую секунду.
- Звук клика и звук покупки сопровождают действия.
- В UI отображаются две переменные:
  - Общее количество очков (валюта)
  - Доход в секунду
- Небольшая анимация нажатия на колбу (изменение `RectTransform` через кнопку / Transposition).

## Автокликеры

| Название             | Цена | Доход / апгрейд |
|----------------------|------|-----------------|
| 🐀 Rat               | 15   | 0.1             |
| 🐇 Rabbit            | 100  | 0.5             |
| 🐙 Mutated Octopus  | 500  | 4               |
| 👽 Alien Assistant   | 3000 | 4               |
| 🧬 Replicator        | 10000| 40              |

> Цены и коэффициенты задаются вручную в инспекторе Unity, в gif указаны тестовые значения.

## Частота обновления валюты

```csharp
private void Update()
{
    if (nextTimeCheck < Time.timeSinceLevelLoad)
    {
        IdleUpdate();
        nextTimeCheck = Time.timeSinceLevelLoad + (1f / updatesPerSecond);
    }
}
```
## Производство очков

Производство очков происходит с заданной частотой обновлений (`updatesPerSecond`, по умолчанию 5).  
Частота регулируется в инспекторе и определяет, как часто обновляется пассивный доход и UI.

---

## GameManager

- Отвечает за основную механику:
  - Счёт
  - Обновление UI
  - Обработка кликов
  - Расчёт дохода
- Использует `updatesPerSecond` для контроля частоты обновлений.
- Обрабатывает клики, добавляя очки и бонус от пассивного дохода.

---

## UI адаптирован под эталонный размер **1280x720** (`Canvas Scaler: Scale With Screen Size`).
![Демонстрация](Gif/UIScalingDemo.gif)

## ManagerStoreUpgrade

- Управляет конкретным апгрейдом:
  - Отображает цену, доход и доступность покупки
  - Подсвечивает апгрейды, когда игрок может их позволить
- Изначально иконки улучшений скрыты.
- При покупке:
  - Увеличивает уровень
  - Обновляет UI
  - Запускает звук улучшения

---

## Подсветка и скрытие апгрейдов

```csharp
public void UpdateUI()
    {
        startColor.a = canBuy ? 1f : alpha;
        frame.color = startColor;
        button.interactable = canBuy;

        bool isAvailable = currentLevel > 0;
        iconCharacter.color = isAvailable ? Color.white : Color.black;
        nameText.text = isAvailable ? upgradeName : "???";
    }
```
## Бонусы баланса

- Игра спроектирована так, чтобы даже без активной игры доход увеличивался со временем.
- С каждым апгрейдом увеличивается не только доход, но и бонус от ручного клика.

---

## Следующие улучшения

- Сцена меню  
- Система сохранения и загрузки прогресса
- Подсчет кликов игроком в секунду
- Настройки звука  
- Система бустов
- Больше тем для автоклика и визуальное разнообразие помощников
